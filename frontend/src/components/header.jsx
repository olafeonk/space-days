import Container from "react-bootstrap/Container";
import Row from "react-bootstrap/Row";

const Header = () => (
  <Container>
    <Row className="header">Это шапка</Row>
  </Container>
);

export default Header;
